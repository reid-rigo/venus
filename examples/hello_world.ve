// hello world with various Venus features

fn greet(name) {
  "hello ${name}"
}

let message = greet("world")
print(message)

// multiline string
let multi = """line one
line two"""
print(multi)

// string methods
print(string.len("hello"))

// safe nav
let t = nil
print(t?.x)
