# Venus

Syntax highlighting for [Venus](https://github.com/reid/venus) (`.vs`) files — a small language that compiles to LuaJIT.

## Installing

```sh
ln -s "$PWD/vscode-venus" ~/.vscode/extensions/venus-lang.vscode-venus
```

Then reload VS Code (Command Palette → "Developer: Reload Window").
